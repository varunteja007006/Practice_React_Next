const CustomHookForFetchAgain = (url) => {
  // try {
  //     const response = await fetch(url);
  //   const data = response.data;
  //   return data;
  // } catch (error) {
  //   console.log(error);
  // }
  const message = "Boom 💣";
  return message;
};

export default CustomHookForFetchAgain;
