import React from "react";

function RandomJsTopics() {
  const data = {
    name: "Varun",
    age: 23,
    score: 0,
  };
  const data2 = 0;

  // console.log(data || "This line wont show up - || (OR)");
  // console.log(data && "This line will show up - && (AND)");
  // console.log(
  //   data2 || "data2 has value 0 (zero) hence right side of || (OR) is executed"
  // );
  // console.log(
  //   data2 && "data2 has value 0 (zero) hence right side of && (AND) is executed"
  // );
  // console.log(
  //   "This is nullish coalescing - ",
  //   data.score ?? "Oops missing value!!"
  // );


  return <div></div>;
}

export default RandomJsTopics;
